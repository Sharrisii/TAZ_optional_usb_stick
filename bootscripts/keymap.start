#!/bin/bash

# This file is triggered by the ubs_keymap.start file in /usb_stick_keymap_lookup/ in the TAZ distro (livecd/liveusb).
# Commands in the /bootscripts folder have priority over the configuration commands placed in /etc/local.d
# So, altering these will change your the settings loaded by the TAZ livecd/usb

setxkbmap -layout be
